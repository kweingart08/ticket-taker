<h2> <%= @movie.title %> </h2>

<hr>
<%= link_to "Edit", edit_movie_path(@movie), :class => 'button' %>
<%= link_to "Delete",
  movie_path(@movie),
  method: :delete,
  :class => 'button btn-danger' %>
